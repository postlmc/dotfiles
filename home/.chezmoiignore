{{- $ageKeyPath := joinPath .chezmoi.homeDir ".config" "chezmoi" "key.txt" }}
{{- if not (stat $ageKeyPath) }}
# age identity key not present — skip encrypted files rather than failing
.config/dotfiles.local/**
{{- end }}
