# Bootstrap tool — must stay in Homebrew so it can manage everything else
brew "chezmoi"

{{- if eq .chezmoi.os "darwin" }}

# Newer bash (macOS ships 3.2.x)
brew "bash"

# System networking
brew "iproute2mac"

# Developer tools that must stay in Homebrew (brew-add appends here)
brew "dotnet"
brew "openvpn"

# macOS-specific utilities
brew "age-plugin-se"

# Fonts
cask "font-fira-code-nerd-font"
cask "font-monaspace"
cask "font-sf-mono-nerd-font-ligaturized"

# Security
cask "1password-cli"
cask "git-credential-manager"
cask "keepassxc"

# GUI applications (brew-add --cask appends here)
cask "bruno"
cask "qlmarkdown"
cask "visual-studio-code"

{{- if dig "machine" "personal" false . }}
# Virtualization (personal lab only)
brew "qemu"
brew "vde"

# Database servers (personal dev only — work machines use remote DBs)
brew "postgresql@14"
brew "mysql"

# Security monitoring
cask "knockknock"
cask "osquery"
cask "taskexplorer"

# Personal GUI applications
cask "claude-code"
cask "packetsender"
cask "wireshark-app"
{{- else }}
# PostgreSQL client for connecting to remote databases
brew "postgresql@15"
{{- end }}

# AWS
cask "copilot-cli"

{{- end }}

{{- if dig "development" "docker" false . }}
# Container tools
brew "podman"
brew "podman-compose"
{{- if eq .chezmoi.os "darwin" }}
cask "podman-desktop"
{{- end }}
{{- end }}

{{- if dig "cloud" "azure" false . }}
# Azure tools (Homebrew per Microsoft's recommendations)
tap "azure/functions"
tap "azure/kubelogin"
brew "azure-cli"
brew "azcopy"
brew "aztfexport"
brew "azure/functions/azure-functions-core-tools@4"
brew "azure/kubelogin/kubelogin"
{{- end }}

{{- if dig "cloud" "gcloud" false . }}
# Google Cloud tools
cask "google-cloud-sdk"
{{- end }}

{{- if dig "cloud" "aws" false . }}
# AWS tools
brew "awscli"
{{- end }}

{{- if dig "development" "kubernetes" false . }}
# Kubernetes tools not available in nixpkgs on macOS
brew "calicoctl"
{{- if eq .chezmoi.os "darwin" }}
cask "lens"
{{- end }}
{{- end }}

{{- if dig "tools" "sdr" false . }}
# SDR / hardware driver stack
{{- if eq .chezmoi.os "darwin" }}
brew "airspy"
brew "airspyhf"
brew "esptool"
brew "f3"
brew "hackrf"
brew "soapyrtlsdr"
brew "soapysdr"
{{- end }}
{{- end }}
